---
import '../styles/global.css';
import '../styles/overrides.css';
export interface Props { lang: string; }
const { lang } = Astro.props;
---

<!doctype html>
<html lang={lang}>
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" href="/favicon.ico" />
		<meta name="generator" content={Astro.generator} />
		<title>Thomas Fey-Grytnes · Portfolio</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/katex@0.16.9/dist/katex.min.css" />
  <style>
    /* Obsidian-style tables */
    .prose table {
      border-collapse: separate;
      border-spacing: 0 10px;
    }
    .prose th,
    .prose td {
      border: 1px solid #94a3b8;
      padding: 1rem 1.25rem;
      vertical-align: middle;
    }
    .prose tbody tr:nth-child(even) {
      background: #f8fafc;
    }
    .prose thead th {
      position: sticky;
      top: 0;
      background: #f1f5f9;
    }
  </style>
	</head>
	<body>
    <nav class="language-switcher bg-white/50 backdrop-blur-md py-2 px-4 fixed top-0 right-0 rounded-bl-lg shadow-md z-50">
      <ul class="flex items-center space-x-2">
        <li>
          <a href={`/${lang === 'de' ? 'en' : 'de'}/`} class="flex items-center space-x-1">
            <span class="text-xl">{lang === 'de' ? '🇬🇧' : '🇩🇪'}</span>
            <span class="font-medium">{lang === 'de' ? 'English' : 'Deutsch'}</span>
          </a>
        </li>
      </ul>
    </nav>
		<slot />
	</body>
</html>

<style>
	html,
	body {
		margin: 0;
		width: 100%;
		height: 100%;
	}
</style>
